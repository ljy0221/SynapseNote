export interface UpdateNoteResponse {
  noteId: string;
  title: string;
  invitationUrl: string;
  directoryPath: string;
  pointX: number;
  pointY: number;
  createdAt: string;
  updatedAt: string;
}
